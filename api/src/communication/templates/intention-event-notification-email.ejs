<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      line-height: 1.6;
      color: #313132;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background-color: #003366;
      color: white;
      padding: 20px;
      margin-bottom: 20px;
    }
    .header h1 {
      margin: 0;
      font-size: 24px;
      font-weight: 600;
    }
    .content {
      background-color: #f8f9fa;
      padding: 20px;
      border-left: 4px solid #003366;
      margin-bottom: 20px;
    }
    .outcome-success {
      border-left-color: #2e8540;
    }
    .outcome-failure {
      border-left-color: #d8292f;
    }
    .outcome-unknown {
      border-left-color: #f9b115;
    }
    .detail-row {
      margin: 10px 0;
    }
    .label {
      font-weight: 600;
      color: #606060;
      display: inline-block;
      min-width: 120px;
    }
    .value {
      color: #313132;
    }
    .status-badge {
      display: inline-block;
      padding: 4px 12px;
      border-radius: 3px;
      font-weight: 600;
      font-size: 14px;
      text-transform: uppercase;
    }
    .status-success {
      background-color: #d4edda;
      color: #155724;
    }
    .status-failure {
      background-color: #f8d7da;
      color: #721c24;
    }
    .status-unknown {
      background-color: #fff3cd;
      color: #856404;
    }
    .footer {
      margin-top: 30px;
      padding-top: 20px;
      border-top: 1px solid #e0e0e0;
      font-size: 12px;
      color: #606060;
    }
    a {
      color: #1a5a96;
      text-decoration: none;
    }
    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>Application deployment notification</h1>
  </div>

  <div class="content outcome-<%= outcome %>">
    <div class="detail-row">
      <span class="label">Status:</span>
      <span class="status-badge status-<%= outcome %>"><%= outcome %></span>
    </div>

    <div class="detail-row">
      <span class="label">Application:</span>
      <span class="value"><%= serviceName %></span>
    </div>

    <div class="detail-row">
      <span class="label">Project:</span>
      <span class="value"><%= projectName %></span>
    </div>

    <div class="detail-row">
      <span class="label">Environment:</span>
      <span class="value"><%= environmentName %></span>
    </div>

    <div class="detail-row">
      <span class="label">Deployed by:</span>
      <span class="value"><%= userName %></span>
    </div>

    <div class="detail-row">
      <span class="label">Intention ID:</span>
      <span class="value"><%= intention %></span>
    </div>
  </div>

  <p>
    <% if (outcome === 'success') { %>
    The application was successfully deployed to the <%= environmentName %> environment.
    <% } else if (outcome === 'failure') { %>
    The application deployment to the <%= environmentName %> environment failed. Review the intention details for more information.
    <% } else { %>
    The application deployment to the <%= environmentName %> environment completed with an unknown status. Review the intention details to verify the deployment state.
    <% } %>
  </p>

  <div class="footer">
    <p>
      You received this notification because you are watching this application for deployment events.
      To manage your notification preferences, visit the Broker dashboard.
    </p>
  </div>
</body>
</html>
