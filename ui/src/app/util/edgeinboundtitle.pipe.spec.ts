import { EdgeinboundtitlePipe } from './edgeinboundtitle.pipe';

describe('EdgeinboundtitlePipe', () => {
  it('create an instance', () => {
    const pipe = new EdgeinboundtitlePipe();
    expect(pipe).toBeTruthy();
  });
});
