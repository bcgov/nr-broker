import { EdgetitleinboundPipe } from './edgetitleinbound.pipe';

describe('EdgetitleinboundPipe', () => {
  it('create an instance', () => {
    const pipe = new EdgetitleinboundPipe();
    expect(pipe).toBeTruthy();
  });
});
